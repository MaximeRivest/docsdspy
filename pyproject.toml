[project]
name = "docsdspy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy-template-adapter",
]

[tool.uv.sources]
dspy-template-adapter = { path = "../dspy-community-org/dspy_template_adapter" }
